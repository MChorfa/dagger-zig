name: OpenSSF Scorecard

on:
  branch_protection_rule:
  schedule:
    - cron: "30 1 * * 6"
  push:
    branches: [main]
  workflow_dispatch:

permissions: read-all

jobs:
  scorecard:
    name: Scorecard Analysis Pipeline
    if: github.repository == 'MChorfa/dagger-zig'
    runs-on: ubuntu-latest
    continue-on-error: true
    permissions:
      security-events: write
      id-token: write
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false

      - name: Run Dagger compliance pipeline (scorecard)
        uses: dagger/dagger-for-github@496f1b3d8b0d823834c13e67cf8a8e08ca3b9602 # v8.4.0
        with:
          version: "v0.21.0" # match ci/pipeline/dagger.json engineVersion
          verb: call
          module: ./ci/pipeline
          args: compliance --arg-0=. --arg-1=https://github.com/${{ github.repository }} --arg-2=${{ github.ref_name }} --arg-3=${{ github.event.repository.fork }} export --path=/tmp/dagger-artifacts/compliance

      - name: Upload scorecard results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: scorecard-results
          path: /tmp/dagger-artifacts/compliance/
          retention-days: 90

      - name: Upload to code-scanning
        uses: github/codeql-action/upload-sarif@v3.28.12
        with:
          sarif_file: /tmp/dagger-artifacts/compliance/scorecard.sarif
