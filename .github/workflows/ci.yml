name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  actions: read

jobs:
  # Thin orchestration layer — delegates to Dagger for all build/test/lint
  pipeline:
    name: CI Pipeline (via Dagger)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1

      - name: Run CI pipeline
        uses: dagger/dagger-for-github@496f1b3d8b0d823834c13e67cf8a8e08ca3b9602 # v8.4.0
        with:
          version: "v0.21.7" # match ci/pipeline/dagger.json engineVersion
          verb: call
          module: ./ci/pipeline
          args: run --arg-0=.
