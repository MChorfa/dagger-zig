# Query Builder

Internal GraphQL query builder documentation.

## Overview

The query builder constructs GraphQL queries for the Dagger API.

## Implementation

Located in `src/querybuilder.zig`.

## Usage

Typically used internally by the SDK. End users use the high-level API.
