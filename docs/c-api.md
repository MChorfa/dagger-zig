# C FFI

C Foreign Function Interface documentation.

## Overview

dagger-zig can export a C-compatible API for use from other languages.

## Usage

See `include/dagger_zig.h` for the C header.

## Building

Use `zig build -Demit-c` to generate C bindings.
