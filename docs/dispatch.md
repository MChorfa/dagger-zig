# Comptime Dispatch

Module runtime dispatch system.

## Overview

The module system uses Zig's comptime features for zero-cost dispatch.

## Implementation

Located in `src/module/dispatch.zig`.

## Features

- Zero-cost abstractions
- Type-safe function calls
- Compile-time reflection
